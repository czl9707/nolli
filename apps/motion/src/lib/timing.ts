export const FPS = 30;
/** Frames per still image scene (0.6s @ 30fps + margin). */
export const STILL_FRAMES = 18;
/** Fixed fallback budget for a video scene (used when no ffprobed frame count). */
export const scene2Duration = 150;
