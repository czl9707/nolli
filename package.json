{
  "name": "nolli-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter nolli dev",
    "build": "pnpm --filter nolli build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "deploy": "pnpm --filter nolli deploy"
  }
}
